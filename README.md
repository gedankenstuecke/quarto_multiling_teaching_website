# Quarto example

Uses Quarto and Babelquarto to render a multilingual website from a given quarto project.

Configuration happens in `_quarto.yml`

Rendering/building using `babelquarto::render_website(DIRECTORY_TO_PROJECT))`

